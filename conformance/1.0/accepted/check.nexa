function apply<T>(value: T, transform: (value: T) => T): T {
  return transform(value);
}

function main(): Unit {
  const increment = (value: Int): Int => value + 1;
  print(apply(41, increment));
}
